

 // criar novo elemento a ser adcionado na pagina createElement() 
 //  elementos de texto, inserir o texto no elemento createTextNode(
 // tem a tarefa de indicar relação pai e filho entre os elementos que compõem a árvore DOM. appendChild()


 /*
 - Manipulando Tabelas 

 tags html para criar tabela -> 
 table - definir a tabela
 tr - criar linha
 td - criar coluna
 th - define célula de cabeçalho da tabela


 criar elementos através do javascript

indicar que um texto deve ser filho de td(tabela) td filho de tr(linha) e tr filho de table(tabela em si)


metodos para manipulação de tabela no javascript

insertRow(num) -> inserir linha
insertCell(num) -> inserir coluna

recebe como parâmetro a posição da linha ou coluna a ser inserida na tabela
0 identifica a linha ou coluna incial

inserir uma linha no final da tabela deve se passar o argumento com o valor -1

deletar uma linha da tabela utiliza-se o metodo deleteRow(num)

usando propriedade length para recuperar numeros de linhas da tabela

table.rows.length -> retorna numero de linhas

recuperando valor da tabela - utiliza-se de indices.

table.rows[0].cells[0] recupera o conteúdo da primeira célula da tabela.
*/ 

